import { Link } from 'react-router-dom'; import { auditScores, seedAudits, seedWebsites, categoryLabels } from '../../data/mock'; import { Card, Score, Progress } from '../../components/ui'; import { formatDate } from '../../lib/format'
export function Dashboard(){ const audit=seedAudits[seedAudits.length-1]; return <div className="stack"><div className="page-heading"><div><span className="eyebrow">Overview</span><h1>Your digital presence at a glance.</h1><p>See what matters most, fix the highest-impact issues and track progress over time.</p></div><Link className="btn btn-primary" to="/app/audits/new">Run an audit</Link></div><div className="hero-metrics"><Card className="score-card"><div><span className="muted">Overall health</span><Score value={audit.score} label="Good"/></div><div className="metric-copy"><span className="eyebrow">Latest audit</span><strong>{seedWebsites[0].name}</strong><p>{formatDate(audit.createdAt)} · {audit.durationMs}ms audit runtime.</p><Link to={`/app/audits/${audit.id}`}>View audit →</Link></div></Card><Card><span className="muted">Websites</span><strong className="big-number">{seedWebsites.length}</strong><p>1 site audited this week</p><Link to="/app/websites">Manage websites →</Link></Card><Card><span className="muted">Open recommendations</span><strong className="big-number">{audit.issues.filter(i=>i.status!=='resolved').length}</strong><p>Prioritised by impact and effort</p><Link to="/app/recommendations">View recommendations →</Link></Card></div><div className="grid-2"><Card><div className="section-head"><div><span className="eyebrow">Health by domain</span><h2>Scores that mean something</h2></div><Link to="/app/performance">Details →</Link></div><div className="score-list">{auditScores.map(s=><div className="score-row" key={s.category}><span>{categoryLabels[s.category]}</span><Progress value={s.score}/><strong>{s.score}</strong></div>)}</div></Card><Card><div className="section-head"><div><span className="eyebrow">Next actions</span><h2>Start with the biggest wins</h2></div></div><div className="action-list">{audit.issues.filter(i=>i.status!=='resolved').sort((a,b)=>b.priority-a.priority).slice(0,4).map(i=><Link className="action" key={i.id} to="/app/recommendations"><span className={`dot ${i.severity}`}/><span><strong>{i.title}</strong><small>{i.category} · {i.effort} effort · priority {i.priority}</small></span><span>→</span></Link>)}</div></Card></div></div>}
